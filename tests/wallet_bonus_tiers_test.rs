// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn wallet_bonus_tiers_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_wallet_bonus_tiers_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .wallet_bonus_tiers()
        .list(sellapp::resources::wallet_bonus_tiers::ListParams::default())
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn wallet_bonus_tiers_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .list(sellapp::resources::wallet_bonus_tiers::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn wallet_bonus_tiers_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .list(sellapp::resources::wallet_bonus_tiers::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn wallet_bonus_tiers_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .list(sellapp::resources::wallet_bonus_tiers::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn wallet_bonus_tiers_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .list(sellapp::resources::wallet_bonus_tiers::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn wallet_bonus_tiers_create_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_wallet_bonus_tier_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn wallet_bonus_tiers_create_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn wallet_bonus_tiers_create_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn wallet_bonus_tiers_create_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn wallet_bonus_tiers_create_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn wallet_bonus_tiers_create_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn wallet_bonus_tiers_create_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .create(sellapp::resources::wallet_bonus_tiers::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_bonus_tier_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletBonusTierRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_wallet_bonus_tier_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn wallet_bonus_tiers_replace_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .replace(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn wallet_bonus_tiers_update_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_wallet_bonus_tier_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn wallet_bonus_tiers_update_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn wallet_bonus_tiers_update_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn wallet_bonus_tiers_update_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn wallet_bonus_tiers_update_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn wallet_bonus_tiers_update_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn wallet_bonus_tiers_update_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .update(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_wallet_bonus_tier_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateWalletBonusTierRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_archive_wallet_bonus_tier_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn wallet_bonus_tiers_archive_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .archive(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::ArchiveParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_restore_wallet_bonus_tier_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn wallet_bonus_tiers_restore_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/wallet-bonus-tiers/test_id/restore"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .wallet_bonus_tiers()
        .restore(
            "test_id",
            sellapp::resources::wallet_bonus_tiers::RestoreParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
