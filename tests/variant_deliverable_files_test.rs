// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn variant_deliverable_files_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_variant_deliverable_files_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_deliverable_files()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::ListParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_deliverable_files_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_deliverable_files_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_deliverable_files_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_deliverable_files_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_deliverable_files_list_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/products/test_id/variants/test_id/deliverable/files"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .variant_deliverable_files()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::ListParams::default(),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn variant_deliverable_files_upload_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_upload_variant_deliverable_file_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_deliverable_files_upload_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_deliverable_files_upload_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_deliverable_files_upload_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_deliverable_files_upload_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_deliverable_files_upload_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_deliverable_files_upload_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .upload(
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UploadParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_upload_variant_deliverable_file_request_multipart_form_data.json"
                ))
                .expect(
                    "parse fixture for SdkUploadVariantDeliverableFileRequestMultipartFormData",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn variant_deliverable_files_get_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_variant_deliverable_file_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_deliverable_files()
        .get(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::GetParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_deliverable_files_get_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .get(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_deliverable_files_get_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .get(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_deliverable_files_get_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .get(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_deliverable_files_get_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .get(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_deliverable_files_replace_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/products/test_id/variants/test_id/deliverable/files/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn variant_deliverable_files_replace_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_deliverable_files_replace_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_deliverable_files_replace_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_deliverable_files_replace_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_deliverable_files_replace_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_deliverable_files_replace_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.variant_deliverable_files().replace("test_id", "test_id", "test_id", sellapp::resources::variant_deliverable_files::ReplaceParams::new(serde_json::from_str(include_str!("fixtures/sdk_replace_variant_deliverable_file_settings_request_application_json.json")).expect("parse fixture for SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn variant_deliverable_files_update_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_variant_deliverable_file_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_deliverable_files_update_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_deliverable_files_update_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_deliverable_files_update_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_deliverable_files_update_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_deliverable_files_update_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_deliverable_files_update_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .update(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_variant_deliverable_file_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateVariantDeliverableFileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn variant_deliverable_files_delete_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_delete_variant_deliverable_file_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_deliverable_files_delete_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_deliverable_files_delete_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_deliverable_files_delete_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_deliverable_files_delete_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_deliverable_files_delete_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_deliverable_files_delete_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/deliverable/files/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_deliverable_files()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_deliverable_files::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
