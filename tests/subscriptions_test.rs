// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn subscriptions_cancel_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_cancel_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_cancel_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_cancel_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_cancel_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_cancel_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_cancel_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_cancel_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/subscriptions/test_id/cancel"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel(
            "test_id",
            sellapp::resources::subscriptions::CancelParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_get_capabilities_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id/capabilities"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_subscription_capabilities_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .get_capabilities(
            "test_id",
            sellapp::resources::subscriptions::GetCapabilitiesParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_get_capabilities_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id/capabilities"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_capabilities(
            "test_id",
            sellapp::resources::subscriptions::GetCapabilitiesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_get_capabilities_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id/capabilities"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_capabilities(
            "test_id",
            sellapp::resources::subscriptions::GetCapabilitiesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_get_capabilities_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id/capabilities"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_capabilities(
            "test_id",
            sellapp::resources::subscriptions::GetCapabilitiesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_get_capabilities_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id/capabilities"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_capabilities(
            "test_id",
            sellapp::resources::subscriptions::GetCapabilitiesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/cancel-period-end"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_cancel_subscription_at_period_end_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_cancel_at_period_end_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_at_period_end(
            "test_id",
            sellapp::resources::subscriptions::CancelAtPeriodEndParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_at_period_end_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionAtPeriodEndRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_cancel_immediately_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_cancel_subscription_immediately_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_cancel_immediately_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_cancel_immediately_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_cancel_immediately_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_cancel_immediately_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_cancel_immediately_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_cancel_immediately_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .cancel_immediately(
            "test_id",
            sellapp::resources::subscriptions::CancelImmediatelyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_cancel_subscription_immediately_request_application_json.json"
                ))
                .expect("parse fixture for SdkCancelSubscriptionImmediatelyRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_pause_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_pause_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_pause_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_pause_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_pause_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_pause_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_pause_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_pause_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/pause"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .pause(
            "test_id",
            sellapp::resources::subscriptions::PauseParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_resume_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_resume_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_resume_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_resume_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_resume_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_resume_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_resume_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_resume_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/resume"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .resume(
            "test_id",
            sellapp::resources::subscriptions::ResumeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_update_payment_method_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("\"test_value\""))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_update_payment_method_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_update_payment_method_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_update_payment_method_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_update_payment_method_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_update_payment_method_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_update_payment_method_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .update_payment_method(
            "test_id",
            sellapp::resources::subscriptions::UpdatePaymentMethodParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_subscription_payment_method_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkUpdateSubscriptionPaymentMethodRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_preview_plan_change_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("\"test_value\""))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_preview_plan_change_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_preview_plan_change_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_preview_plan_change_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_preview_plan_change_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_preview_plan_change_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_preview_plan_change_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .preview_plan_change(
            "test_id",
            sellapp::resources::subscriptions::PreviewPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_preview_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkPreviewSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("\"test_value\""))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_confirm_plan_change_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .confirm_plan_change(
            "test_id",
            sellapp::resources::subscriptions::ConfirmPlanChangeParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_confirm_subscription_plan_change_request_application_json.json"
                ))
                .expect("parse fixture for SdkConfirmSubscriptionPlanChangeRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/change-renewal-date/preview"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_preview_renewal_date_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().preview_renewal_date("test_id", sellapp::resources::subscriptions::PreviewRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/test_id/actions/change-renewal-date/confirm"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_confirm_renewal_date_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.subscriptions().confirm_renewal_date("test_id", sellapp::resources::subscriptions::ConfirmRenewalDateParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_list_subscriptions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_subscriptions_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .list_subscriptions(sellapp::resources::subscriptions::ListSubscriptionsParams::default())
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_list_subscriptions_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .list_subscriptions(sellapp::resources::subscriptions::ListSubscriptionsParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_list_subscriptions_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .list_subscriptions(sellapp::resources::subscriptions::ListSubscriptionsParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_list_subscriptions_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .list_subscriptions(sellapp::resources::subscriptions::ListSubscriptionsParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_list_subscriptions_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .list_subscriptions(sellapp::resources::subscriptions::ListSubscriptionsParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_list_subscriptions_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .subscriptions()
        .list_subscriptions(sellapp::resources::subscriptions::ListSubscriptionsParams::default())
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn subscriptions_search_subscriptions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_search_subscriptions_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_search_subscriptions_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_search_subscriptions_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_search_subscriptions_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_search_subscriptions_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn subscriptions_search_subscriptions_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn subscriptions_search_subscriptions_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn subscriptions_search_subscriptions_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/subscriptions/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .subscriptions()
        .search_subscriptions(
            sellapp::resources::subscriptions::SearchSubscriptionsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_subscriptions_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchSubscriptionsRequestApplicationJson"),
            ),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn subscriptions_get_subscription_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .subscriptions()
        .get_subscription(
            "test_id",
            sellapp::resources::subscriptions::GetSubscriptionParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn subscriptions_get_subscription_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_subscription(
            "test_id",
            sellapp::resources::subscriptions::GetSubscriptionParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn subscriptions_get_subscription_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_subscription(
            "test_id",
            sellapp::resources::subscriptions::GetSubscriptionParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn subscriptions_get_subscription_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_subscription(
            "test_id",
            sellapp::resources::subscriptions::GetSubscriptionParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn subscriptions_get_subscription_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .subscriptions()
        .get_subscription(
            "test_id",
            sellapp::resources::subscriptions::GetSubscriptionParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}
