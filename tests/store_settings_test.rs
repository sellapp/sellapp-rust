// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn store_settings_get_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/store/settings"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_store_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .get(sellapp::resources::store_settings::GetParams::default())
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_get_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/store/settings"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .get(sellapp::resources::store_settings::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_get_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/store/settings"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .get(sellapp::resources::store_settings::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_get_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/store/settings"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .get(sellapp::resources::store_settings::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_get_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/store/settings"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .get(sellapp::resources::store_settings::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_replace_general_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_general_store_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_replace_general_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_replace_general_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_replace_general_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_replace_general_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_replace_general_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn store_settings_replace_general_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_general(
            sellapp::resources::store_settings::ReplaceGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn store_settings_update_general_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_general_store_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_update_general_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_update_general_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_update_general_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_update_general_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_update_general_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn store_settings_update_general_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/general"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_general(
            sellapp::resources::store_settings::UpdateGeneralParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_general_store_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateGeneralStoreSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn store_settings_replace_analytics_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_analytics_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_replace_analytics_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_replace_analytics_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_replace_analytics_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_replace_analytics_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_replace_analytics_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn store_settings_replace_analytics_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_analytics(
            sellapp::resources::store_settings::ReplaceAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn store_settings_update_analytics_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_analytics_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_update_analytics_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_update_analytics_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_update_analytics_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_update_analytics_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_update_analytics_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn store_settings_update_analytics_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/analytics"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_analytics(
            sellapp::resources::store_settings::UpdateAnalyticsParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_analytics_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateAnalyticsSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn store_settings_replace_marketing_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_marketing_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_replace_marketing_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_replace_marketing_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_replace_marketing_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_replace_marketing_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_replace_marketing_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn store_settings_replace_marketing_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .replace_marketing(
            sellapp::resources::store_settings::ReplaceMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn store_settings_update_marketing_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_marketing_settings_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn store_settings_update_marketing_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn store_settings_update_marketing_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn store_settings_update_marketing_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn store_settings_update_marketing_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn store_settings_update_marketing_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn store_settings_update_marketing_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/store/settings/marketing"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .store_settings()
        .update_marketing(
            sellapp::resources::store_settings::UpdateMarketingParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_marketing_settings_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateMarketingSettingsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
