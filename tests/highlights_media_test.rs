// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn highlights_media_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_highlight_media_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .list(
            "test_id",
            sellapp::resources::highlights_media::ListParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .list(
            "test_id",
            sellapp::resources::highlights_media::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .list(
            "test_id",
            sellapp::resources::highlights_media::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .list(
            "test_id",
            sellapp::resources::highlights_media::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .list(
            "test_id",
            sellapp::resources::highlights_media::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_add_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_add_highlight_media_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_add_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_add_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_add_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_add_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_add_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn highlights_media_add_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .add(
            "test_id",
            sellapp::resources::highlights_media::AddParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_add_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkAddHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn highlights_media_reorder_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_reorder_highlight_media_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_reorder_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_reorder_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_reorder_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_reorder_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_reorder_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn highlights_media_reorder_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .reorder(
            "test_id",
            sellapp::resources::highlights_media::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_highlights_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderHighlightsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn highlights_media_replace_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_highlight_media_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_replace_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_replace_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_replace_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_replace_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_replace_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn highlights_media_replace_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn highlights_media_replace_metadata_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_highlight_media_metadata_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_replace_metadata_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_replace_metadata_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_replace_metadata_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_replace_metadata_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_replace_metadata_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn highlights_media_replace_metadata_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .replace_metadata(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::ReplaceMetadataParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_highlight_media_metadata_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceHighlightMediaMetadataRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn highlights_media_update_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_highlight_media_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_update_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_update_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_update_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_update_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_update_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn highlights_media_update_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_highlight_media_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateHighlightMediaRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn highlights_media_delete_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_delete_highlight_media_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn highlights_media_delete_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn highlights_media_delete_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn highlights_media_delete_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn highlights_media_delete_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn highlights_media_delete_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn highlights_media_delete_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/highlights/test_id/media/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .highlights_media()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::highlights_media::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
