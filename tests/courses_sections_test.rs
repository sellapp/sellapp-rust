// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn courses_sections_create_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_course_section_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn courses_sections_create_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn courses_sections_create_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn courses_sections_create_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn courses_sections_create_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn courses_sections_create_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn courses_sections_create_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/courses/test_id/sections"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .create(
            "test_id",
            sellapp::resources::courses_sections::CreateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn courses_sections_replace_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_course_section_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn courses_sections_replace_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn courses_sections_replace_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn courses_sections_replace_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn courses_sections_replace_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn courses_sections_replace_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn courses_sections_replace_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn courses_sections_update_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_course_section_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn courses_sections_update_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn courses_sections_update_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn courses_sections_update_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn courses_sections_update_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn courses_sections_update_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn courses_sections_update_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_course_section_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCourseSectionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn courses_sections_delete_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_delete_course_section_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn courses_sections_delete_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn courses_sections_delete_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn courses_sections_delete_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn courses_sections_delete_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn courses_sections_delete_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn courses_sections_delete_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/v2/courses/test_id/sections/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .delete(
            "test_id",
            "test_id",
            sellapp::resources::courses_sections::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn courses_sections_reorder_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_reorder_course_sections_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn courses_sections_reorder_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn courses_sections_reorder_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn courses_sections_reorder_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn courses_sections_reorder_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn courses_sections_reorder_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn courses_sections_reorder_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/courses/test_id/sections/reorder"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .courses_sections()
        .reorder(
            "test_id",
            sellapp::resources::courses_sections::ReorderParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reorder_course_sections_request_application_json.json"
                ))
                .expect("parse fixture for SdkReorderCourseSectionsRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
