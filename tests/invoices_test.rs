// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn invoices_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_invoices_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .list(sellapp::resources::invoices::ListParams::default())
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .list(sellapp::resources::invoices::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .list(sellapp::resources::invoices::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .list(sellapp::resources::invoices::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .list(sellapp::resources::invoices::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_list_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(
            ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{},\"meta\":{}}"),
        )
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .invoices()
        .list(sellapp::resources::invoices::ListParams::default())
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn invoices_create_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_invoice_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_create_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_create_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_create_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_create_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_create_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_create_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create(sellapp::resources::invoices::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_invoice_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateInvoiceRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_search_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_search_invoices_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_search_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_search_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_search_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_search_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_search_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_search_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_search_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/search"))
        .respond_with(
            ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{},\"meta\":{}}"),
        )
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .invoices()
        .search(sellapp::resources::invoices::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_invoices_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchInvoicesRequestApplicationJson"),
        ))
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn invoices_get_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_invoice_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .get(
            "test_id",
            sellapp::resources::invoices::GetParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_get_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get(
            "test_id",
            sellapp::resources::invoices::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_get_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get(
            "test_id",
            sellapp::resources::invoices::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_get_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get(
            "test_id",
            sellapp::resources::invoices::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_get_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get(
            "test_id",
            sellapp::resources::invoices::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_go_to_checkout_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_checkout_session_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_go_to_checkout_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_go_to_checkout_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_go_to_checkout_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_go_to_checkout_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_go_to_checkout_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_go_to_checkout_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .go_to_checkout(
            "test_id",
            sellapp::resources::invoices::GoToCheckoutParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_get_deliverables_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id/deliverables"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_invoice_deliverables_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .get_deliverables(
            "test_id",
            sellapp::resources::invoices::GetDeliverablesParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_get_deliverables_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get_deliverables(
            "test_id",
            sellapp::resources::invoices::GetDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_get_deliverables_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get_deliverables(
            "test_id",
            sellapp::resources::invoices::GetDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_get_deliverables_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get_deliverables(
            "test_id",
            sellapp::resources::invoices::GetDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_get_deliverables_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/invoices/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .get_deliverables(
            "test_id",
            sellapp::resources::invoices::GetDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_mark_completed_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_mark_pending_invoice_completed_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_mark_completed_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_mark_completed_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_mark_completed_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_mark_completed_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_mark_completed_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_mark_completed_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-completed"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_completed(
            "test_id",
            sellapp::resources::invoices::MarkCompletedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_completed_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceCompletedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_mark_voided_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_mark_pending_invoice_voided_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_mark_voided_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_mark_voided_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_mark_voided_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_mark_voided_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_mark_voided_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_mark_voided_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/mark-voided"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .mark_voided(
            "test_id",
            sellapp::resources::invoices::MarkVoidedParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_mark_pending_invoice_voided_request_application_json.json"
                ))
                .expect("parse fixture for SdkMarkPendingInvoiceVoidedRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_issue_replacement_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn invoices_issue_replacement_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_issue_replacement_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_issue_replacement_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_issue_replacement_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_issue_replacement_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_issue_replacement_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/issue-replacement"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.invoices().issue_replacement("test_id", sellapp::resources::invoices::IssueReplacementParams::new(serde_json::from_str(include_str!("fixtures/sdk_issue_replacement_for_completed_invoice_request_application_json.json")).expect("parse fixture for SdkIssueReplacementForCompletedInvoiceRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_update_status_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_invoice_status_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_update_status_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_update_status_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_update_status_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_update_status_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_update_status_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_update_status_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/invoices/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .update_status(
            "test_id",
            sellapp::resources::invoices::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_invoice_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateInvoiceStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_create_refund_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_invoice_refund_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_create_refund_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_create_refund_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_create_refund_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_create_refund_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_create_refund_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_create_refund_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .create_refund(
            "test_id",
            sellapp::resources::invoices::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_invoice_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateInvoiceRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_retry_fulfillment_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_fulfillment_retry_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_retry_fulfillment_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_retry_fulfillment_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_retry_fulfillment_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_retry_fulfillment_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_retry_fulfillment_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_retry_fulfillment_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/invoices/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::invoices::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateFulfillmentRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_dynamic_delivery_retry_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_retry_dynamic_delivery_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/dynamic-delivery-retries",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::invoices::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_dynamic_delivery_retry_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateDynamicDeliveryRetryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn invoices_notify_fulfillment_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_fulfillment_notifications_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn invoices_notify_fulfillment_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn invoices_notify_fulfillment_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn invoices_notify_fulfillment_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn invoices_notify_fulfillment_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn invoices_notify_fulfillment_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn invoices_notify_fulfillment_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/invoices/test_id/fulfillment-notifications",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .invoices()
        .notify_fulfillment(
            "test_id",
            sellapp::resources::invoices::NotifyFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_fulfillment_notifications_request_application_json.json"
                ))
                .expect(
                    "parse fixture for SdkCreateFulfillmentNotificationsRequestApplicationJson",
                ),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
