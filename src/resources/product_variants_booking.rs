// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct ProductVariantsBookingApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct ListAvailabilityParams {
    /// Start date for the availability window.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub from: Option<String>,
    /// End date for the availability window.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub to: Option<String>,
    /// Number of seats to reserve for each returned slot.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub quantity: Option<i64>,
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct CreateHoldParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkCreateBookingHoldRequestApplicationJson,
}

impl CreateHoldParams {
    /// Construct a new `CreateHoldParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkCreateBookingHoldRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReleaseHoldParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReleaseBookingHoldRequestApplicationJson,
}

impl ReleaseHoldParams {
    /// Construct a new `ReleaseHoldParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReleaseBookingHoldRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct GetParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplaceParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplaceBookingConfigurationRequestApplicationJson,
}

impl ReplaceParams {
    /// Construct a new `ReplaceParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplaceBookingConfigurationRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkUpdateBookingConfigurationRequestApplicationJson,
}

impl UpdateParams {
    /// Construct a new `UpdateParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkUpdateBookingConfigurationRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

impl<'a> ProductVariantsBookingApi<'a> {
    /// List booking availability
    ///
    /// Retrieve available appointment slots for a booking product variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn list_availability(
        &self,
        product: &str,
        variant: &str,
        params: ListAvailabilityParams,
    ) -> Result<SdkListBookingAvailabilityResponseValue200ApplicationJson, Error> {
        self.list_availability_with_options(product, variant, params, None)
            .await
    }

    /// Variant of [`Self::list_availability`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_availability_with_options(
        &self,
        product: &str,
        variant: &str,
        params: ListAvailabilityParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkListBookingAvailabilityResponseValue200ApplicationJson, Error> {
        self.list_availability_raw(product, variant, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn list_availability_raw(
        &self,
        product: &str,
        variant: &str,
        params: ListAvailabilityParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkListBookingAvailabilityResponseValue200ApplicationJson>, Error>
    {
        let product = crate::client::path_segment(product);
        let variant = crate::client::path_segment(variant);
        let path = format!("/v2/products/{product}/variants/{variant}/booking/availability");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("listBookingAvailability".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/products/{product}/variants/{variant}/booking/availability",
            )
            .await
    }

    /// Create a booking hold
    ///
    /// Temporarily reserve a booking slot before checkout. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn create_hold(
        &self,
        product: &str,
        variant: &str,
        params: CreateHoldParams,
    ) -> Result<SdkCreateBookingHoldResponseValue201ApplicationJson, Error> {
        self.create_hold_with_options(product, variant, params, None)
            .await
    }

    /// Variant of [`Self::create_hold`] that accepts per-request [`crate::RequestOptions`].
    pub async fn create_hold_with_options(
        &self,
        product: &str,
        variant: &str,
        params: CreateHoldParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkCreateBookingHoldResponseValue201ApplicationJson, Error> {
        self.create_hold_raw(product, variant, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn create_hold_raw(
        &self,
        product: &str,
        variant: &str,
        params: CreateHoldParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkCreateBookingHoldResponseValue201ApplicationJson>, Error>
    {
        let product = crate::client::path_segment(product);
        let variant = crate::client::path_segment(variant);
        let path = format!("/v2/products/{product}/variants/{variant}/booking/holds");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("createBookingHold".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/products/{product}/variants/{variant}/booking/holds",
            )
            .await
    }

    /// Release a booking hold
    ///
    /// Release a previously created booking hold. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn release_hold(
        &self,
        product: &str,
        hold: &str,
        params: ReleaseHoldParams,
    ) -> Result<SdkReleaseBookingHoldResponseValue200ApplicationJson, Error> {
        self.release_hold_with_options(product, hold, params, None)
            .await
    }

    /// Variant of [`Self::release_hold`] that accepts per-request [`crate::RequestOptions`].
    pub async fn release_hold_with_options(
        &self,
        product: &str,
        hold: &str,
        params: ReleaseHoldParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReleaseBookingHoldResponseValue200ApplicationJson, Error> {
        self.release_hold_raw(product, hold, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn release_hold_raw(
        &self,
        product: &str,
        hold: &str,
        params: ReleaseHoldParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReleaseBookingHoldResponseValue200ApplicationJson>, Error>
    {
        let product = crate::client::path_segment(product);
        let hold = crate::client::path_segment(hold);
        let path = format!("/v2/products/{product}/variants/booking/holds/{hold}");
        let method = http::Method::DELETE;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("releaseBookingHold".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "DELETE /v2/products/{product}/variants/booking/holds/{hold}",
            )
            .await
    }

    /// Retrieve booking configuration
    ///
    /// Retrieve creator-facing availability, capacity, calendar, meeting, and reminder configuration for a booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn get(
        &self,
        product: &str,
        variant: &str,
        params: GetParams,
    ) -> Result<SdkGetBookingConfigurationResponseValue200ApplicationJson, Error> {
        self.get_with_options(product, variant, params, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        product: &str,
        variant: &str,
        params: GetParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkGetBookingConfigurationResponseValue200ApplicationJson, Error> {
        self.get_raw(product, variant, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn get_raw(
        &self,
        product: &str,
        variant: &str,
        params: GetParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkGetBookingConfigurationResponseValue200ApplicationJson>, Error>
    {
        let product = crate::client::path_segment(product);
        let variant = crate::client::path_segment(variant);
        let path = format!("/v2/products/{product}/variants/{variant}/booking");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("getBookingConfiguration".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/products/{product}/variants/{variant}/booking",
            )
            .await
    }

    /// Update booking configuration
    ///
    /// Partially update a booking variant's configuration. Provider connection IDs must belong to the selected store; secret credentials are never accepted or returned here. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn replace(
        &self,
        product: &str,
        variant: &str,
        params: ReplaceParams,
    ) -> Result<SdkReplaceBookingConfigurationResponseValue200ApplicationJson, Error> {
        self.replace_with_options(product, variant, params, None)
            .await
    }

    /// Variant of [`Self::replace`] that accepts per-request [`crate::RequestOptions`].
    pub async fn replace_with_options(
        &self,
        product: &str,
        variant: &str,
        params: ReplaceParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplaceBookingConfigurationResponseValue200ApplicationJson, Error> {
        self.replace_raw(product, variant, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn replace_raw(
        &self,
        product: &str,
        variant: &str,
        params: ReplaceParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<
        crate::RawResponse<SdkReplaceBookingConfigurationResponseValue200ApplicationJson>,
        Error,
    > {
        let product = crate::client::path_segment(product);
        let variant = crate::client::path_segment(variant);
        let path = format!("/v2/products/{product}/variants/{variant}/booking");
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replaceBookingConfiguration".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/products/{product}/variants/{variant}/booking",
            )
            .await
    }

    /// Update booking configuration
    ///
    /// Partially update a booking variant's configuration. Provider connection IDs must belong to the selected store; secret credentials are never accepted or returned here. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn update(
        &self,
        product: &str,
        variant: &str,
        params: UpdateParams,
    ) -> Result<SdkUpdateBookingConfigurationResponseValue200ApplicationJson, Error> {
        self.update_with_options(product, variant, params, None)
            .await
    }

    /// Variant of [`Self::update`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_with_options(
        &self,
        product: &str,
        variant: &str,
        params: UpdateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkUpdateBookingConfigurationResponseValue200ApplicationJson, Error> {
        self.update_raw(product, variant, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn update_raw(
        &self,
        product: &str,
        variant: &str,
        params: UpdateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<
        crate::RawResponse<SdkUpdateBookingConfigurationResponseValue200ApplicationJson>,
        Error,
    > {
        let product = crate::client::path_segment(product);
        let variant = crate::client::path_segment(variant);
        let path = format!("/v2/products/{product}/variants/{variant}/booking");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("updateBookingConfiguration".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/products/{product}/variants/{variant}/booking",
            )
            .await
    }
}
