// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn product_variants_booking_list_availability_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/availability",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_booking_availability_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .product_variants_booking()
        .list_availability(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ListAvailabilityParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn product_variants_booking_list_availability_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/availability",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .list_availability(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ListAvailabilityParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn product_variants_booking_list_availability_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/availability",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .list_availability(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ListAvailabilityParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn product_variants_booking_list_availability_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/availability",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .list_availability(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ListAvailabilityParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn product_variants_booking_list_availability_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/availability",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .list_availability(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ListAvailabilityParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn product_variants_booking_create_hold_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_booking_hold_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn product_variants_booking_create_hold_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn product_variants_booking_create_hold_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn product_variants_booking_create_hold_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn product_variants_booking_create_hold_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn product_variants_booking_create_hold_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn product_variants_booking_create_hold_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking/holds",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .create_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::CreateHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn product_variants_booking_release_hold_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_release_booking_hold_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn product_variants_booking_release_hold_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn product_variants_booking_release_hold_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn product_variants_booking_release_hold_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn product_variants_booking_release_hold_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn product_variants_booking_release_hold_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn product_variants_booking_release_hold_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/booking/holds/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .release_hold(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReleaseHoldParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_release_booking_hold_request_application_json.json"
                ))
                .expect("parse fixture for SdkReleaseBookingHoldRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn product_variants_booking_get_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_booking_configuration_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .product_variants_booking()
        .get(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::GetParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn product_variants_booking_get_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .get(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn product_variants_booking_get_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .get(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn product_variants_booking_get_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .get(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn product_variants_booking_get_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .get(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::GetParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn product_variants_booking_replace_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_booking_configuration_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn product_variants_booking_replace_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn product_variants_booking_replace_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn product_variants_booking_replace_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn product_variants_booking_replace_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn product_variants_booking_replace_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn product_variants_booking_replace_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn product_variants_booking_update_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_booking_configuration_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn product_variants_booking_update_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn product_variants_booking_update_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn product_variants_booking_update_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn product_variants_booking_update_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn product_variants_booking_update_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn product_variants_booking_update_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/booking",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .product_variants_booking()
        .update(
            "test_id",
            "test_id",
            sellapp::resources::product_variants_booking::UpdateParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_booking_configuration_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateBookingConfigurationRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
