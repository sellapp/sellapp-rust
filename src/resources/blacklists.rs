// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct BlacklistsApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Serialize)]
pub struct ListParams {
    /// Number of items to return per page.
    ///
    /// Defaults to `15`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Page number to return.
    ///
    /// Defaults to `1`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
}

impl Default for ListParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            limit: Some(15),
            page: Some(1),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct CreateParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: BlacklistCreateRequest,
}

impl CreateParams {
    /// Construct a new `CreateParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: BlacklistCreateRequest) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: BlacklistUpdateRequest,
}

impl UpdateParams {
    /// Construct a new `UpdateParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: BlacklistUpdateRequest) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct V2ListBlacklistsParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct V2CreateBlacklistParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: BlacklistCreateRequest,
}

impl V2CreateBlacklistParams {
    /// Construct a new `V2CreateBlacklistParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: BlacklistCreateRequest) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct V2GetBlacklistParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct V2ReplaceBlacklistParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: BlacklistUpdateRequest,
}

impl V2ReplaceBlacklistParams {
    /// Construct a new `V2ReplaceBlacklistParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: BlacklistUpdateRequest) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2UpdateBlacklistParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: BlacklistUpdateRequest,
}

impl V2UpdateBlacklistParams {
    /// Construct a new `V2UpdateBlacklistParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: BlacklistUpdateRequest) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct V2DeleteBlacklistParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

impl<'a> BlacklistsApi<'a> {
    /// List all blacklist rules
    ///
    /// Retrieve a paginated list of blacklist rules for your store. By default, a maximum of fifteen blacklist rules are returned per page.
    pub async fn list(&self, params: ListParams) -> Result<BlacklistListResponse, Error> {
        self.list_with_options(params, None).await
    }

    /// Variant of [`Self::list`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_with_options(
        &self,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistListResponse, Error> {
        self.list_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn list_raw(
        &self,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistListResponse>, Error> {
        let path = "/v1/blacklists".to_string();
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("listBlacklists".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v1/blacklists",
            )
            .await
    }

    /// Create a blacklist rule
    ///
    /// Create a new blacklist rule for your store.
    pub async fn create(&self, params: CreateParams) -> Result<BlacklistResponse, Error> {
        self.create_with_options(params, None).await
    }

    /// Variant of [`Self::create`] that accepts per-request [`crate::RequestOptions`].
    pub async fn create_with_options(
        &self,
        params: CreateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.create_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn create_raw(
        &self,
        params: CreateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let path = "/v1/blacklists".to_string();
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("createBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v1/blacklists",
            )
            .await
    }

    /// Retrieve a blacklist rule
    ///
    /// Retrieve a specific blacklist rule by its unique identifier.
    pub async fn get(&self, blacklist: &str) -> Result<BlacklistResponse, Error> {
        self.get_with_options(blacklist, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        blacklist: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.get_raw(blacklist, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn get_raw(
        &self,
        blacklist: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v1/blacklists/{blacklist}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("getBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &(),
                options,
                "GET /v1/blacklists/{blacklist}",
            )
            .await
    }

    /// Update a blacklist rule
    ///
    /// Update one or more attributes on an existing blacklist rule.
    pub async fn update(
        &self,
        blacklist: &str,
        params: UpdateParams,
    ) -> Result<BlacklistResponse, Error> {
        self.update_with_options(blacklist, params, None).await
    }

    /// Variant of [`Self::update`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_with_options(
        &self,
        blacklist: &str,
        params: UpdateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.update_raw(blacklist, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn update_raw(
        &self,
        blacklist: &str,
        params: UpdateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v1/blacklists/{blacklist}");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("updateBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v1/blacklists/{blacklist}",
            )
            .await
    }

    /// Delete a blacklist rule
    ///
    /// Permanently delete a blacklist rule from your store.
    pub async fn delete(&self, blacklist: &str) -> Result<BlacklistResponse, Error> {
        self.delete_with_options(blacklist, None).await
    }

    /// Variant of [`Self::delete`] that accepts per-request [`crate::RequestOptions`].
    pub async fn delete_with_options(
        &self,
        blacklist: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.delete_raw(blacklist, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn delete_raw(
        &self,
        blacklist: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v1/blacklists/{blacklist}");
        let method = http::Method::DELETE;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("deleteBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &(),
                options,
                "DELETE /v1/blacklists/{blacklist}",
            )
            .await
    }

    /// List blacklist rules
    ///
    /// List store-scoped blacklist rules. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_list_blacklists(
        &self,
        params: V2ListBlacklistsParams,
    ) -> Result<BlacklistListResponse, Error> {
        self.v_2_list_blacklists_with_options(params, None).await
    }

    /// Variant of [`Self::v_2_list_blacklists`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_list_blacklists_with_options(
        &self,
        params: V2ListBlacklistsParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistListResponse, Error> {
        self.v_2_list_blacklists_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_list_blacklists_raw(
        &self,
        params: V2ListBlacklistsParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistListResponse>, Error> {
        let path = "/v2/blacklists".to_string();
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2ListBlacklists".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/blacklists",
            )
            .await
    }

    /// Create a blacklist rule
    ///
    /// Create a store-scoped blacklist rule. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_create_blacklist(
        &self,
        params: V2CreateBlacklistParams,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_create_blacklist_with_options(params, None).await
    }

    /// Variant of [`Self::v_2_create_blacklist`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_create_blacklist_with_options(
        &self,
        params: V2CreateBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_create_blacklist_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_create_blacklist_raw(
        &self,
        params: V2CreateBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let path = "/v2/blacklists".to_string();
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = true;
        merged.operation_id = Some("v2CreateBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/blacklists",
            )
            .await
    }

    /// Retrieve a blacklist rule
    ///
    /// Retrieve a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_get_blacklist(
        &self,
        blacklist: &str,
        params: V2GetBlacklistParams,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_get_blacklist_with_options(blacklist, params, None)
            .await
    }

    /// Variant of [`Self::v_2_get_blacklist`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_get_blacklist_with_options(
        &self,
        blacklist: &str,
        params: V2GetBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_get_blacklist_raw(blacklist, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_get_blacklist_raw(
        &self,
        blacklist: &str,
        params: V2GetBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v2/blacklists/{blacklist}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2GetBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/blacklists/{blacklist}",
            )
            .await
    }

    /// Replace a blacklist rule
    ///
    /// Replace a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_replace_blacklist(
        &self,
        blacklist: &str,
        params: V2ReplaceBlacklistParams,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_replace_blacklist_with_options(blacklist, params, None)
            .await
    }

    /// Variant of [`Self::v_2_replace_blacklist`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_replace_blacklist_with_options(
        &self,
        blacklist: &str,
        params: V2ReplaceBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_replace_blacklist_raw(blacklist, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_replace_blacklist_raw(
        &self,
        blacklist: &str,
        params: V2ReplaceBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v2/blacklists/{blacklist}");
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = true;
        merged.operation_id = Some("v2ReplaceBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/blacklists/{blacklist}",
            )
            .await
    }

    /// Update a blacklist rule
    ///
    /// Update a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_update_blacklist(
        &self,
        blacklist: &str,
        params: V2UpdateBlacklistParams,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_update_blacklist_with_options(blacklist, params, None)
            .await
    }

    /// Variant of [`Self::v_2_update_blacklist`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_update_blacklist_with_options(
        &self,
        blacklist: &str,
        params: V2UpdateBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_update_blacklist_raw(blacklist, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_update_blacklist_raw(
        &self,
        blacklist: &str,
        params: V2UpdateBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v2/blacklists/{blacklist}");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = true;
        merged.operation_id = Some("v2UpdateBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/blacklists/{blacklist}",
            )
            .await
    }

    /// Delete a blacklist rule
    ///
    /// Delete a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_delete_blacklist(
        &self,
        blacklist: &str,
        params: V2DeleteBlacklistParams,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_delete_blacklist_with_options(blacklist, params, None)
            .await
    }

    /// Variant of [`Self::v_2_delete_blacklist`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_delete_blacklist_with_options(
        &self,
        blacklist: &str,
        params: V2DeleteBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<BlacklistResponse, Error> {
        self.v_2_delete_blacklist_raw(blacklist, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_delete_blacklist_raw(
        &self,
        blacklist: &str,
        params: V2DeleteBlacklistParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<BlacklistResponse>, Error> {
        let blacklist = crate::client::path_segment(blacklist);
        let path = format!("/v2/blacklists/{blacklist}");
        let method = http::Method::DELETE;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2DeleteBlacklist".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "DELETE /v2/blacklists/{blacklist}",
            )
            .await
    }
}
