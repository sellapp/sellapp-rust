// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct BundleItemsApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct GetParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct ListParams {
    /// Number of items to return per page.
    ///
    /// Defaults to `15`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Page number to return.
    ///
    /// Defaults to `1`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

impl Default for ListParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            limit: Some(15),
            page: Some(1),
            x_store: Default::default(),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct AttachParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkAttachBundleItemsRequestApplicationJson,
}

impl AttachParams {
    /// Construct a new `AttachParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkAttachBundleItemsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct DetachParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkDetachBundleItemsRequestApplicationJson,
}

impl DetachParams {
    /// Construct a new `DetachParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkDetachBundleItemsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

impl<'a> BundleItemsApi<'a> {
    /// Retrieve a bundle item
    ///
    /// Retrieve a specific item from a bundle by its product variant ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn get(
        &self,
        bundle: &str,
        item: &str,
        params: GetParams,
    ) -> Result<SdkGetBundleItemResponseValue200ApplicationJson, Error> {
        self.get_with_options(bundle, item, params, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        bundle: &str,
        item: &str,
        params: GetParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkGetBundleItemResponseValue200ApplicationJson, Error> {
        self.get_raw(bundle, item, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn get_raw(
        &self,
        bundle: &str,
        item: &str,
        params: GetParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkGetBundleItemResponseValue200ApplicationJson>, Error> {
        let bundle = crate::client::path_segment(bundle);
        let item = crate::client::path_segment(item);
        let path = format!("/v2/bundles/{bundle}/items/{item}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("getBundleItem".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/bundles/{bundle}/items/{item}",
            )
            .await
    }

    /// List bundle items
    ///
    /// Retrieve a paginated list of product variants attached to a bundle. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn list(
        &self,
        bundle: &str,
        params: ListParams,
    ) -> Result<SdkListBundleItemsResponseValue200ApplicationJson, Error> {
        self.list_with_options(bundle, params, None).await
    }

    /// Variant of [`Self::list`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_with_options(
        &self,
        bundle: &str,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkListBundleItemsResponseValue200ApplicationJson, Error> {
        self.list_raw(bundle, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn list_raw(
        &self,
        bundle: &str,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkListBundleItemsResponseValue200ApplicationJson>, Error> {
        let bundle = crate::client::path_segment(bundle);
        let path = format!("/v2/bundles/{bundle}/items");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("listBundleItems".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/bundles/{bundle}/items",
            )
            .await
    }

    /// Attach bundle items
    ///
    /// Attach one or more product variants to a bundle with quantities. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn attach(
        &self,
        bundle: &str,
        params: AttachParams,
    ) -> Result<SdkAttachBundleItemsResponseValue200ApplicationJson, Error> {
        self.attach_with_options(bundle, params, None).await
    }

    /// Variant of [`Self::attach`] that accepts per-request [`crate::RequestOptions`].
    pub async fn attach_with_options(
        &self,
        bundle: &str,
        params: AttachParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkAttachBundleItemsResponseValue200ApplicationJson, Error> {
        self.attach_raw(bundle, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn attach_raw(
        &self,
        bundle: &str,
        params: AttachParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkAttachBundleItemsResponseValue200ApplicationJson>, Error>
    {
        let bundle = crate::client::path_segment(bundle);
        let path = format!("/v2/bundles/{bundle}/items/attach");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("attachBundleItems".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/bundles/{bundle}/items/attach",
            )
            .await
    }

    /// Detach bundle items
    ///
    /// Detach one or more product variants from a bundle. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn detach(
        &self,
        bundle: &str,
        params: DetachParams,
    ) -> Result<SdkDetachBundleItemsResponseValue200ApplicationJson, Error> {
        self.detach_with_options(bundle, params, None).await
    }

    /// Variant of [`Self::detach`] that accepts per-request [`crate::RequestOptions`].
    pub async fn detach_with_options(
        &self,
        bundle: &str,
        params: DetachParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkDetachBundleItemsResponseValue200ApplicationJson, Error> {
        self.detach_raw(bundle, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn detach_raw(
        &self,
        bundle: &str,
        params: DetachParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkDetachBundleItemsResponseValue200ApplicationJson>, Error>
    {
        let bundle = crate::client::path_segment(bundle);
        let path = format!("/v2/bundles/{bundle}/items/detach");
        let method = http::Method::DELETE;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("detachBundleItems".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "DELETE /v2/bundles/{bundle}/items/detach",
            )
            .await
    }
}
