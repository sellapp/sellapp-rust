// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn tickets_messages_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_ticket_messages_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .list(
            "test_id",
            sellapp::resources::tickets_messages::ListParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .list(
            "test_id",
            sellapp::resources::tickets_messages::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .list(
            "test_id",
            sellapp::resources::tickets_messages::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .list(
            "test_id",
            sellapp::resources::tickets_messages::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .list(
            "test_id",
            sellapp::resources::tickets_messages::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_list_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(
            ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{},\"meta\":{}}"),
        )
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .tickets_messages()
        .list(
            "test_id",
            sellapp::resources::tickets_messages::ListParams::default(),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn tickets_messages_reply_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_reply_to_ticket_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_reply_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_reply_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_reply_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_reply_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_reply_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn tickets_messages_reply_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .reply(
            "test_id",
            sellapp::resources::tickets_messages::ReplyParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn tickets_messages_get_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_ticket_message_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .get("test_id", "test_id")
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_get_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .get("test_id", "test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_get_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .get("test_id", "test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_get_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .get("test_id", "test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_get_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v1/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .get("test_id", "test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_search_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_search_ticket_messages_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_search_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_search_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_search_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_search_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_search_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn tickets_messages_search_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn tickets_messages_search_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v1/tickets/test_id/messages/search"))
        .respond_with(
            ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{},\"meta\":{}}"),
        )
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .tickets_messages()
        .search(
            "test_id",
            sellapp::resources::tickets_messages::SearchParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkSearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn tickets_messages_v_2_list_ticket_messages_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_v_2_list_ticket_messages_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .v_2_list_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2ListTicketMessagesParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_v_2_list_ticket_messages_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_list_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2ListTicketMessagesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_v_2_list_ticket_messages_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_list_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2ListTicketMessagesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_v_2_list_ticket_messages_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_list_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2ListTicketMessagesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_v_2_list_ticket_messages_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_list_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2ListTicketMessagesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_v_2_list_ticket_messages_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(
            ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{},\"meta\":{}}"),
        )
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .tickets_messages()
        .v_2_list_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2ListTicketMessagesParams::default(),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_v_2_reply_to_ticket_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn tickets_messages_v_2_reply_to_ticket_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_reply_to_ticket(
            "test_id",
            sellapp::resources::tickets_messages::V2ReplyToTicketParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_reply_to_ticket_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2ReplyToTicketRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_v_2_search_ticket_messages_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn tickets_messages_v_2_search_ticket_messages_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/tickets/test_id/messages/search"))
        .respond_with(
            ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{},\"meta\":{}}"),
        )
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .tickets_messages()
        .v_2_search_ticket_messages(
            "test_id",
            sellapp::resources::tickets_messages::V2SearchTicketMessagesParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_v_2_search_ticket_messages_request_application_json.json"
                ))
                .expect("parse fixture for SdkV2SearchTicketMessagesRequestApplicationJson"),
            ),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn tickets_messages_v_2_get_ticket_message_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_v_2_get_ticket_message_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .tickets_messages()
        .v_2_get_ticket_message(
            "test_id",
            "test_id",
            sellapp::resources::tickets_messages::V2GetTicketMessageParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn tickets_messages_v_2_get_ticket_message_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_get_ticket_message(
            "test_id",
            "test_id",
            sellapp::resources::tickets_messages::V2GetTicketMessageParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn tickets_messages_v_2_get_ticket_message_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_get_ticket_message(
            "test_id",
            "test_id",
            sellapp::resources::tickets_messages::V2GetTicketMessageParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn tickets_messages_v_2_get_ticket_message_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_get_ticket_message(
            "test_id",
            "test_id",
            sellapp::resources::tickets_messages::V2GetTicketMessageParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn tickets_messages_v_2_get_ticket_message_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/tickets/test_id/messages/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .tickets_messages()
        .v_2_get_ticket_message(
            "test_id",
            "test_id",
            sellapp::resources::tickets_messages::V2GetTicketMessageParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}
