// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct TicketsMessagesApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Serialize)]
pub struct ListParams {
    /// Number of items to return per page.
    ///
    /// Defaults to `15`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Page number to return.
    ///
    /// Defaults to `1`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
}

impl Default for ListParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            limit: Some(15),
            page: Some(1),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplyParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplyToTicketRequestApplicationJson,
}

impl ReplyParams {
    /// Construct a new `ReplyParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplyToTicketRequestApplicationJson) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct SearchParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkSearchTicketMessagesRequestApplicationJson,
}

impl SearchParams {
    /// Construct a new `SearchParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkSearchTicketMessagesRequestApplicationJson) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2ListTicketMessagesParams {
    /// Number of items to return per page.
    ///
    /// Defaults to `15`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Page number to return.
    ///
    /// Defaults to `1`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

impl Default for V2ListTicketMessagesParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            limit: Some(15),
            page: Some(1),
            x_store: Default::default(),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2ReplyToTicketParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkV2ReplyToTicketRequestApplicationJson,
}

impl V2ReplyToTicketParams {
    /// Construct a new `V2ReplyToTicketParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkV2ReplyToTicketRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2SearchTicketMessagesParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkV2SearchTicketMessagesRequestApplicationJson,
}

impl V2SearchTicketMessagesParams {
    /// Construct a new `V2SearchTicketMessagesParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkV2SearchTicketMessagesRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct V2GetTicketMessageParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

impl<'a> TicketsMessagesApi<'a> {
    /// List all ticket messages
    ///
    /// Read a ticket's conversation, 15 messages per page by default.
    pub async fn list(
        &self,
        ticket: &str,
        params: ListParams,
    ) -> Result<SdkListTicketMessagesResponseValue200ApplicationJson, Error> {
        self.list_with_options(ticket, params, None).await
    }

    /// Variant of [`Self::list`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_with_options(
        &self,
        ticket: &str,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkListTicketMessagesResponseValue200ApplicationJson, Error> {
        self.list_raw(ticket, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn list_raw(
        &self,
        ticket: &str,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkListTicketMessagesResponseValue200ApplicationJson>, Error>
    {
        let ticket = crate::client::path_segment(ticket);
        let path = format!("/v1/tickets/{ticket}/messages");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("listTicketMessages".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v1/tickets/{ticket}/messages",
            )
            .await
    }

    /// Reply to ticket
    ///
    /// Add a seller reply to a support conversation.
    pub async fn reply(
        &self,
        ticket: &str,
        params: ReplyParams,
    ) -> Result<SdkReplyToTicketResponseValue201ApplicationJson, Error> {
        self.reply_with_options(ticket, params, None).await
    }

    /// Variant of [`Self::reply`] that accepts per-request [`crate::RequestOptions`].
    pub async fn reply_with_options(
        &self,
        ticket: &str,
        params: ReplyParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplyToTicketResponseValue201ApplicationJson, Error> {
        self.reply_raw(ticket, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn reply_raw(
        &self,
        ticket: &str,
        params: ReplyParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReplyToTicketResponseValue201ApplicationJson>, Error> {
        let ticket = crate::client::path_segment(ticket);
        let path = format!("/v1/tickets/{ticket}/messages");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replyToTicket".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v1/tickets/{ticket}/messages",
            )
            .await
    }

    /// Retrieve specific ticket message
    ///
    /// Retrieve one message using its ticket ID and message ID.
    pub async fn get(
        &self,
        ticket: &str,
        message: &str,
    ) -> Result<SdkGetTicketMessageResponseValue200ApplicationJson, Error> {
        self.get_with_options(ticket, message, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        ticket: &str,
        message: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkGetTicketMessageResponseValue200ApplicationJson, Error> {
        self.get_raw(ticket, message, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn get_raw(
        &self,
        ticket: &str,
        message: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkGetTicketMessageResponseValue200ApplicationJson>, Error> {
        let ticket = crate::client::path_segment(ticket);
        let message = crate::client::path_segment(message);
        let path = format!("/v1/tickets/{ticket}/messages/{message}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("getTicketMessage".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &(),
                options,
                "GET /v1/tickets/{ticket}/messages/{message}",
            )
            .await
    }

    /// Search ticket messages
    ///
    /// Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions.
    pub async fn search(
        &self,
        ticket: &str,
        params: SearchParams,
    ) -> Result<SdkSearchTicketMessagesResponseValue200ApplicationJson, Error> {
        self.search_with_options(ticket, params, None).await
    }

    /// Variant of [`Self::search`] that accepts per-request [`crate::RequestOptions`].
    pub async fn search_with_options(
        &self,
        ticket: &str,
        params: SearchParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkSearchTicketMessagesResponseValue200ApplicationJson, Error> {
        self.search_raw(ticket, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn search_raw(
        &self,
        ticket: &str,
        params: SearchParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkSearchTicketMessagesResponseValue200ApplicationJson>, Error>
    {
        let ticket = crate::client::path_segment(ticket);
        let path = format!("/v1/tickets/{ticket}/messages/search");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("searchTicketMessages".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v1/tickets/{ticket}/messages/search",
            )
            .await
    }

    /// List all ticket messages
    ///
    /// Read a ticket's conversation, 15 messages per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_list_ticket_messages(
        &self,
        ticket: &str,
        params: V2ListTicketMessagesParams,
    ) -> Result<SdkV2ListTicketMessagesResponseValue200ApplicationJson, Error> {
        self.v_2_list_ticket_messages_with_options(ticket, params, None)
            .await
    }

    /// Variant of [`Self::v_2_list_ticket_messages`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_list_ticket_messages_with_options(
        &self,
        ticket: &str,
        params: V2ListTicketMessagesParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2ListTicketMessagesResponseValue200ApplicationJson, Error> {
        self.v_2_list_ticket_messages_raw(ticket, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_list_ticket_messages_raw(
        &self,
        ticket: &str,
        params: V2ListTicketMessagesParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2ListTicketMessagesResponseValue200ApplicationJson>, Error>
    {
        let ticket = crate::client::path_segment(ticket);
        let path = format!("/v2/tickets/{ticket}/messages");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2ListTicketMessages".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/tickets/{ticket}/messages",
            )
            .await
    }

    /// Reply to ticket
    ///
    /// Add a seller reply to a support conversation. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_reply_to_ticket(
        &self,
        ticket: &str,
        params: V2ReplyToTicketParams,
    ) -> Result<SdkV2ReplyToTicketResponseValue201ApplicationJson, Error> {
        self.v_2_reply_to_ticket_with_options(ticket, params, None)
            .await
    }

    /// Variant of [`Self::v_2_reply_to_ticket`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_reply_to_ticket_with_options(
        &self,
        ticket: &str,
        params: V2ReplyToTicketParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2ReplyToTicketResponseValue201ApplicationJson, Error> {
        self.v_2_reply_to_ticket_raw(ticket, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_reply_to_ticket_raw(
        &self,
        ticket: &str,
        params: V2ReplyToTicketParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2ReplyToTicketResponseValue201ApplicationJson>, Error> {
        let ticket = crate::client::path_segment(ticket);
        let path = format!("/v2/tickets/{ticket}/messages");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2ReplyToTicket".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/tickets/{ticket}/messages",
            )
            .await
    }

    /// Search ticket messages
    ///
    /// Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_search_ticket_messages(
        &self,
        ticket: &str,
        params: V2SearchTicketMessagesParams,
    ) -> Result<SdkV2SearchTicketMessagesResponseValue200ApplicationJson, Error> {
        self.v_2_search_ticket_messages_with_options(ticket, params, None)
            .await
    }

    /// Variant of [`Self::v_2_search_ticket_messages`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_search_ticket_messages_with_options(
        &self,
        ticket: &str,
        params: V2SearchTicketMessagesParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2SearchTicketMessagesResponseValue200ApplicationJson, Error> {
        self.v_2_search_ticket_messages_raw(ticket, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_search_ticket_messages_raw(
        &self,
        ticket: &str,
        params: V2SearchTicketMessagesParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2SearchTicketMessagesResponseValue200ApplicationJson>, Error>
    {
        let ticket = crate::client::path_segment(ticket);
        let path = format!("/v2/tickets/{ticket}/messages/search");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2SearchTicketMessages".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/tickets/{ticket}/messages/search",
            )
            .await
    }

    /// Retrieve specific ticket message
    ///
    /// Retrieve one message using its ticket ID and message ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_get_ticket_message(
        &self,
        ticket: &str,
        message: &str,
        params: V2GetTicketMessageParams,
    ) -> Result<SdkV2GetTicketMessageResponseValue200ApplicationJson, Error> {
        self.v_2_get_ticket_message_with_options(ticket, message, params, None)
            .await
    }

    /// Variant of [`Self::v_2_get_ticket_message`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_get_ticket_message_with_options(
        &self,
        ticket: &str,
        message: &str,
        params: V2GetTicketMessageParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2GetTicketMessageResponseValue200ApplicationJson, Error> {
        self.v_2_get_ticket_message_raw(ticket, message, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_get_ticket_message_raw(
        &self,
        ticket: &str,
        message: &str,
        params: V2GetTicketMessageParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2GetTicketMessageResponseValue200ApplicationJson>, Error>
    {
        let ticket = crate::client::path_segment(ticket);
        let message = crate::client::path_segment(message);
        let path = format!("/v2/tickets/{ticket}/messages/{message}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2GetTicketMessage".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/tickets/{ticket}/messages/{message}",
            )
            .await
    }
}
