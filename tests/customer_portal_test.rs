// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn customer_portal_get_customer_portal_profile_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_customer_portal_profile_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .get_customer_portal_profile()
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_get_customer_portal_profile_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_profile()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_profile_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_profile()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_profile_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_profile()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_get_customer_portal_profile_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_profile()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_customer_portal_profile_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_update_customer_portal_profile_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/customer-portal/me"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .update_customer_portal_profile(
            sellapp::resources::customer_portal::UpdateCustomerPortalProfileParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_customer_portal_profile_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateCustomerPortalProfileRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_orders_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_customer_portal_orders_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .list_customer_portal_orders()
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_list_customer_portal_orders_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_orders()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_orders_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_orders()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_orders_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_orders()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_list_customer_portal_orders_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_orders()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_orders_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .customer_portal()
        .list_customer_portal_orders()
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn customer_portal_get_customer_portal_order_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_customer_portal_order_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .get_customer_portal_order("test_id")
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_get_customer_portal_order_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_order("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_order_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_order("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_order_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_order("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_get_customer_portal_order_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_order("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_subscriptions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_list_customer_portal_subscriptions_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .list_customer_portal_subscriptions()
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_list_customer_portal_subscriptions_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_subscriptions()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_subscriptions_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_subscriptions()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_subscriptions_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_subscriptions()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_list_customer_portal_subscriptions_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_subscriptions()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_customer_portal_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .get_customer_portal_subscription("test_id")
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_capabilities_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/capabilities"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_get_customer_portal_subscription_capabilities_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .get_customer_portal_subscription_capabilities("test_id")
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_capabilities_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/capabilities",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription_capabilities("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_capabilities_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/capabilities",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription_capabilities("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_capabilities_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/capabilities",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription_capabilities("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_get_customer_portal_subscription_capabilities_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/capabilities",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .get_customer_portal_subscription_capabilities("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_entitlements_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/entitlements"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_list_customer_portal_entitlements_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .list_customer_portal_entitlements()
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_list_customer_portal_entitlements_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/entitlements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_entitlements()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_entitlements_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/entitlements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_entitlements()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_list_customer_portal_entitlements_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/entitlements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_entitlements()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_list_customer_portal_entitlements_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/customer-portal/entitlements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .list_customer_portal_entitlements()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_at_period_end_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-period-end",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_at_period_end("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionAtPeriodEndParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_at_period_end_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionAtPeriodEndRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_cancel_customer_subscription_immediately_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/cancel-immediately",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().cancel_customer_subscription_immediately("test_id", sellapp::resources::customer_portal::CancelCustomerSubscriptionImmediatelyParams::new(serde_json::from_str(include_str!("fixtures/sdk_cancel_customer_subscription_immediately_request_application_json.json")).expect("parse fixture for SdkCancelCustomerSubscriptionImmediatelyRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_pause_customer_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_pause_customer_subscription_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/pause",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .pause_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::PauseCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pause_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkPauseCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_resume_customer_subscription_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_resume_customer_subscription_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/resume",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .customer_portal()
        .resume_customer_subscription(
            "test_id",
            sellapp::resources::customer_portal::ResumeCustomerSubscriptionParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_resume_customer_subscription_request_application_json.json"
                ))
                .expect("parse fixture for SdkResumeCustomerSubscriptionRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/update-payment-method"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_update_customer_subscription_payment_method_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_update_customer_subscription_payment_method_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/update-payment-method",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().update_customer_subscription_payment_method("test_id", sellapp::resources::customer_portal::UpdateCustomerSubscriptionPaymentMethodParams::new(serde_json::from_str(include_str!("fixtures/sdk_update_customer_subscription_payment_method_request_application_json.json")).expect("parse fixture for SdkUpdateCustomerSubscriptionPaymentMethodRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_plan_change_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_plan_change_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-plan/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_plan_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionPlanChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_plan_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionPlanChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_preview_customer_subscription_renewal_date_change_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/preview",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().preview_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::PreviewCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_preview_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkPreviewCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn customer_portal_confirm_customer_subscription_renewal_date_change_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/customer-portal/subscriptions/test_id/actions/change-renewal-date/confirm",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.customer_portal().confirm_customer_subscription_renewal_date_change("test_id", sellapp::resources::customer_portal::ConfirmCustomerSubscriptionRenewalDateChangeParams::new(serde_json::from_str(include_str!("fixtures/sdk_confirm_customer_subscription_renewal_date_change_request_application_json.json")).expect("parse fixture for SdkConfirmCustomerSubscriptionRenewalDateChangeRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
