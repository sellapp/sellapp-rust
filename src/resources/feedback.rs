// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct FeedbackApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Serialize)]
pub struct ListParams {
    /// Number of items to return per page.
    ///
    /// Defaults to `15`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Page number to return.
    ///
    /// Defaults to `1`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
}

impl Default for ListParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            limit: Some(15),
            page: Some(1),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplyParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplyToFeedbackRequestApplicationJson,
}

impl ReplyParams {
    /// Construct a new `ReplyParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplyToFeedbackRequestApplicationJson) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct SearchParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkSearchFeedbackRequestApplicationJson,
}

impl SearchParams {
    /// Construct a new `SearchParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkSearchFeedbackRequestApplicationJson) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2ListFeedbackParams {
    /// Number of items to return per page.
    ///
    /// Defaults to `15`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Page number to return.
    ///
    /// Defaults to `1`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

impl Default for V2ListFeedbackParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            limit: Some(15),
            page: Some(1),
            x_store: Default::default(),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2SearchFeedbackParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkV2SearchFeedbackRequestApplicationJson,
}

impl V2SearchFeedbackParams {
    /// Construct a new `V2SearchFeedbackParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkV2SearchFeedbackRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct V2GetFeedbackParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct V2ReplaceFeedbackParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkV2ReplaceFeedbackRequestApplicationJson,
}

impl V2ReplaceFeedbackParams {
    /// Construct a new `V2ReplaceFeedbackParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkV2ReplaceFeedbackRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct V2ReplyToFeedbackParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkV2ReplyToFeedbackRequestApplicationJson,
}

impl V2ReplyToFeedbackParams {
    /// Construct a new `V2ReplyToFeedbackParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkV2ReplyToFeedbackRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

impl<'a> FeedbackApi<'a> {
    /// List all feedback
    ///
    /// List customer feedback for your store, 15 entries per page by default.
    pub async fn list(
        &self,
        params: ListParams,
    ) -> Result<SdkListFeedbackResponseValue200ApplicationJson, Error> {
        self.list_with_options(params, None).await
    }

    /// Variant of [`Self::list`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_with_options(
        &self,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkListFeedbackResponseValue200ApplicationJson, Error> {
        self.list_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn list_raw(
        &self,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkListFeedbackResponseValue200ApplicationJson>, Error> {
        let path = "/v1/feedback".to_string();
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("listFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(method, &path, &params, options, "GET /v1/feedback")
            .await
    }

    /// Retrieve specific feedback
    ///
    /// Retrieve a customer's feedback by its ID.
    pub async fn get(
        &self,
        feedback: &str,
    ) -> Result<SdkGetFeedbackResponseValue200ApplicationJson, Error> {
        self.get_with_options(feedback, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        feedback: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkGetFeedbackResponseValue200ApplicationJson, Error> {
        self.get_raw(feedback, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn get_raw(
        &self,
        feedback: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkGetFeedbackResponseValue200ApplicationJson>, Error> {
        let feedback = crate::client::path_segment(feedback);
        let path = format!("/v1/feedback/{feedback}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("getFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &(),
                options,
                "GET /v1/feedback/{feedback}",
            )
            .await
    }

    /// Reply to feedback
    ///
    /// Publish a seller reply to a customer's feedback.
    pub async fn reply(
        &self,
        feedback: &str,
        params: ReplyParams,
    ) -> Result<SdkReplyToFeedbackResponseValue200ApplicationJson, Error> {
        self.reply_with_options(feedback, params, None).await
    }

    /// Variant of [`Self::reply`] that accepts per-request [`crate::RequestOptions`].
    pub async fn reply_with_options(
        &self,
        feedback: &str,
        params: ReplyParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplyToFeedbackResponseValue200ApplicationJson, Error> {
        self.reply_raw(feedback, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn reply_raw(
        &self,
        feedback: &str,
        params: ReplyParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReplyToFeedbackResponseValue200ApplicationJson>, Error> {
        let feedback = crate::client::path_segment(feedback);
        let path = format!("/v1/feedback/{feedback}");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replyToFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v1/feedback/{feedback}",
            )
            .await
    }

    /// Search feedback
    ///
    /// Search feedback using JSON body filters, search terms, includes, and sort instructions.
    pub async fn search(
        &self,
        params: SearchParams,
    ) -> Result<SdkSearchFeedbackResponseValue200ApplicationJson, Error> {
        self.search_with_options(params, None).await
    }

    /// Variant of [`Self::search`] that accepts per-request [`crate::RequestOptions`].
    pub async fn search_with_options(
        &self,
        params: SearchParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkSearchFeedbackResponseValue200ApplicationJson, Error> {
        self.search_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn search_raw(
        &self,
        params: SearchParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkSearchFeedbackResponseValue200ApplicationJson>, Error> {
        let path = "/v1/feedback/search".to_string();
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("searchFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v1/feedback/search",
            )
            .await
    }

    /// List all feedback
    ///
    /// List customer feedback for your store, 15 entries per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_list_feedback(
        &self,
        params: V2ListFeedbackParams,
    ) -> Result<SdkV2ListFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_list_feedback_with_options(params, None).await
    }

    /// Variant of [`Self::v_2_list_feedback`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_list_feedback_with_options(
        &self,
        params: V2ListFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2ListFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_list_feedback_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_list_feedback_raw(
        &self,
        params: V2ListFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2ListFeedbackResponseValue200ApplicationJson>, Error> {
        let path = "/v2/feedback".to_string();
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2ListFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(method, &path, &params, options, "GET /v2/feedback")
            .await
    }

    /// Search feedback
    ///
    /// Search feedback using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_search_feedback(
        &self,
        params: V2SearchFeedbackParams,
    ) -> Result<SdkV2SearchFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_search_feedback_with_options(params, None).await
    }

    /// Variant of [`Self::v_2_search_feedback`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_search_feedback_with_options(
        &self,
        params: V2SearchFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2SearchFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_search_feedback_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_search_feedback_raw(
        &self,
        params: V2SearchFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2SearchFeedbackResponseValue200ApplicationJson>, Error> {
        let path = "/v2/feedback/search".to_string();
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2SearchFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/feedback/search",
            )
            .await
    }

    /// Retrieve specific feedback
    ///
    /// Retrieve a customer's feedback by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_get_feedback(
        &self,
        feedback: &str,
        params: V2GetFeedbackParams,
    ) -> Result<SdkV2GetFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_get_feedback_with_options(feedback, params, None)
            .await
    }

    /// Variant of [`Self::v_2_get_feedback`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_get_feedback_with_options(
        &self,
        feedback: &str,
        params: V2GetFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2GetFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_get_feedback_raw(feedback, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_get_feedback_raw(
        &self,
        feedback: &str,
        params: V2GetFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2GetFeedbackResponseValue200ApplicationJson>, Error> {
        let feedback = crate::client::path_segment(feedback);
        let path = format!("/v2/feedback/{feedback}");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2GetFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/feedback/{feedback}",
            )
            .await
    }

    /// Reply to feedback
    ///
    /// Publish a seller reply to a customer's feedback. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_replace_feedback(
        &self,
        feedback: &str,
        params: V2ReplaceFeedbackParams,
    ) -> Result<SdkV2ReplaceFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_replace_feedback_with_options(feedback, params, None)
            .await
    }

    /// Variant of [`Self::v_2_replace_feedback`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_replace_feedback_with_options(
        &self,
        feedback: &str,
        params: V2ReplaceFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2ReplaceFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_replace_feedback_raw(feedback, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_replace_feedback_raw(
        &self,
        feedback: &str,
        params: V2ReplaceFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2ReplaceFeedbackResponseValue200ApplicationJson>, Error>
    {
        let feedback = crate::client::path_segment(feedback);
        let path = format!("/v2/feedback/{feedback}");
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2ReplaceFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/feedback/{feedback}",
            )
            .await
    }

    /// Reply to feedback
    ///
    /// Publish a seller reply to a customer's feedback. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn v_2_reply_to_feedback(
        &self,
        feedback: &str,
        params: V2ReplyToFeedbackParams,
    ) -> Result<SdkV2ReplyToFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_reply_to_feedback_with_options(feedback, params, None)
            .await
    }

    /// Variant of [`Self::v_2_reply_to_feedback`] that accepts per-request [`crate::RequestOptions`].
    pub async fn v_2_reply_to_feedback_with_options(
        &self,
        feedback: &str,
        params: V2ReplyToFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkV2ReplyToFeedbackResponseValue200ApplicationJson, Error> {
        self.v_2_reply_to_feedback_raw(feedback, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn v_2_reply_to_feedback_raw(
        &self,
        feedback: &str,
        params: V2ReplyToFeedbackParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkV2ReplyToFeedbackResponseValue200ApplicationJson>, Error>
    {
        let feedback = crate::client::path_segment(feedback);
        let path = format!("/v2/feedback/{feedback}");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("v2ReplyToFeedback".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/feedback/{feedback}",
            )
            .await
    }
}
