// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn variant_serials_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_variant_serial_inventory_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_serials()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ListParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_serials_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_serials_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_serials_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_serials_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ListParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_serials_list_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/products/test_id/variants/test_id/serials"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .variant_serials()
        .list(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ListParams::default(),
        )
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn variant_serials_append_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_append_variant_serial_inventory_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_serials_append_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_serials_append_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_serials_append_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_serials_append_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_serials_append_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_serials_append_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .append(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::AppendParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_append_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkAppendVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn variant_serials_replace_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_replace_variant_serial_inventory_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_serials_replace_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_serials_replace_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_serials_replace_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_serials_replace_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_serials_replace_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_serials_replace_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .replace(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::ReplaceParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_replace_variant_serial_inventory_request_application_json.json"
                ))
                .expect("parse fixture for SdkReplaceVariantSerialInventoryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn variant_serials_queue_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_queue_variant_serial_import_response_value_202_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_serials_queue_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_serials_queue_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_serials_queue_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_serials_queue_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_serials_queue_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_serials_queue_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/import",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .queue(
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::QueueParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_queue_variant_serial_import_request_multipart_form_data.json"
                ))
                .expect("parse fixture for SdkQueueVariantSerialImportRequestMultipartFormData"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn variant_serials_delete_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_delete_variant_serial_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn variant_serials_delete_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn variant_serials_delete_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn variant_serials_delete_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn variant_serials_delete_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn variant_serials_delete_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn variant_serials_delete_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/v2/products/test_id/variants/test_id/serials/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .variant_serials()
        .delete(
            "test_id",
            "test_id",
            "test_id",
            sellapp::resources::variant_serials::DeleteParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
