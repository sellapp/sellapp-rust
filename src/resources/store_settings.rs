// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct StoreSettingsApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct GetParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplaceGeneralParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplaceGeneralStoreSettingsRequestApplicationJson,
}

impl ReplaceGeneralParams {
    /// Construct a new `ReplaceGeneralParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplaceGeneralStoreSettingsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateGeneralParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkUpdateGeneralStoreSettingsRequestApplicationJson,
}

impl UpdateGeneralParams {
    /// Construct a new `UpdateGeneralParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkUpdateGeneralStoreSettingsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplaceAnalyticsParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplaceAnalyticsSettingsRequestApplicationJson,
}

impl ReplaceAnalyticsParams {
    /// Construct a new `ReplaceAnalyticsParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplaceAnalyticsSettingsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateAnalyticsParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkUpdateAnalyticsSettingsRequestApplicationJson,
}

impl UpdateAnalyticsParams {
    /// Construct a new `UpdateAnalyticsParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkUpdateAnalyticsSettingsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplaceMarketingParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplaceMarketingSettingsRequestApplicationJson,
}

impl ReplaceMarketingParams {
    /// Construct a new `ReplaceMarketingParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplaceMarketingSettingsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateMarketingParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkUpdateMarketingSettingsRequestApplicationJson,
}

impl UpdateMarketingParams {
    /// Construct a new `UpdateMarketingParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkUpdateMarketingSettingsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

impl<'a> StoreSettingsApi<'a> {
    /// Retrieve store settings
    ///
    /// Retrieve the current store configuration and status flags. Secret analytics credentials are represented only by configured booleans. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn get(
        &self,
        params: GetParams,
    ) -> Result<SdkGetStoreSettingsResponseValue200ApplicationJson, Error> {
        self.get_with_options(params, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        params: GetParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkGetStoreSettingsResponseValue200ApplicationJson, Error> {
        self.get_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn get_raw(
        &self,
        params: GetParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkGetStoreSettingsResponseValue200ApplicationJson>, Error> {
        let path = "/v2/store/settings".to_string();
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("getStoreSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/store/settings",
            )
            .await
    }

    /// Update general store settings
    ///
    /// Update general store settings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn replace_general(
        &self,
        params: ReplaceGeneralParams,
    ) -> Result<SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson, Error> {
        self.replace_general_with_options(params, None).await
    }

    /// Variant of [`Self::replace_general`] that accepts per-request [`crate::RequestOptions`].
    pub async fn replace_general_with_options(
        &self,
        params: ReplaceGeneralParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson, Error> {
        self.replace_general_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn replace_general_raw(
        &self,
        params: ReplaceGeneralParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<
        crate::RawResponse<SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson>,
        Error,
    > {
        let path = "/v2/store/settings/general".to_string();
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replaceGeneralStoreSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/store/settings/general",
            )
            .await
    }

    /// Update general store settings
    ///
    /// Update general store settings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn update_general(
        &self,
        params: UpdateGeneralParams,
    ) -> Result<SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson, Error> {
        self.update_general_with_options(params, None).await
    }

    /// Variant of [`Self::update_general`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_general_with_options(
        &self,
        params: UpdateGeneralParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson, Error> {
        self.update_general_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn update_general_raw(
        &self,
        params: UpdateGeneralParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<
        crate::RawResponse<SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson>,
        Error,
    > {
        let path = "/v2/store/settings/general".to_string();
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("updateGeneralStoreSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/store/settings/general",
            )
            .await
    }

    /// Update analytics settings
    ///
    /// Update analytics settings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn replace_analytics(
        &self,
        params: ReplaceAnalyticsParams,
    ) -> Result<SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson, Error> {
        self.replace_analytics_with_options(params, None).await
    }

    /// Variant of [`Self::replace_analytics`] that accepts per-request [`crate::RequestOptions`].
    pub async fn replace_analytics_with_options(
        &self,
        params: ReplaceAnalyticsParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson, Error> {
        self.replace_analytics_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn replace_analytics_raw(
        &self,
        params: ReplaceAnalyticsParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson>, Error>
    {
        let path = "/v2/store/settings/analytics".to_string();
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replaceAnalyticsSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/store/settings/analytics",
            )
            .await
    }

    /// Update analytics settings
    ///
    /// Update analytics settings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn update_analytics(
        &self,
        params: UpdateAnalyticsParams,
    ) -> Result<SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson, Error> {
        self.update_analytics_with_options(params, None).await
    }

    /// Variant of [`Self::update_analytics`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_analytics_with_options(
        &self,
        params: UpdateAnalyticsParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson, Error> {
        self.update_analytics_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn update_analytics_raw(
        &self,
        params: UpdateAnalyticsParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson>, Error>
    {
        let path = "/v2/store/settings/analytics".to_string();
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("updateAnalyticsSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/store/settings/analytics",
            )
            .await
    }

    /// Update marketing settings
    ///
    /// Update marketing settings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn replace_marketing(
        &self,
        params: ReplaceMarketingParams,
    ) -> Result<SdkReplaceMarketingSettingsResponseValue200ApplicationJson, Error> {
        self.replace_marketing_with_options(params, None).await
    }

    /// Variant of [`Self::replace_marketing`] that accepts per-request [`crate::RequestOptions`].
    pub async fn replace_marketing_with_options(
        &self,
        params: ReplaceMarketingParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplaceMarketingSettingsResponseValue200ApplicationJson, Error> {
        self.replace_marketing_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn replace_marketing_raw(
        &self,
        params: ReplaceMarketingParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReplaceMarketingSettingsResponseValue200ApplicationJson>, Error>
    {
        let path = "/v2/store/settings/marketing".to_string();
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replaceMarketingSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/store/settings/marketing",
            )
            .await
    }

    /// Update marketing settings
    ///
    /// Update marketing settings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn update_marketing(
        &self,
        params: UpdateMarketingParams,
    ) -> Result<SdkUpdateMarketingSettingsResponseValue200ApplicationJson, Error> {
        self.update_marketing_with_options(params, None).await
    }

    /// Variant of [`Self::update_marketing`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_marketing_with_options(
        &self,
        params: UpdateMarketingParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkUpdateMarketingSettingsResponseValue200ApplicationJson, Error> {
        self.update_marketing_raw(params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn update_marketing_raw(
        &self,
        params: UpdateMarketingParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkUpdateMarketingSettingsResponseValue200ApplicationJson>, Error>
    {
        let path = "/v2/store/settings/marketing".to_string();
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("updateMarketingSettings".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/store/settings/marketing",
            )
            .await
    }
}
