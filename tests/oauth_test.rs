// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn oauth_get_oauth_authorization_server_metadata_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/.well-known/oauth-authorization-server"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_get_oauth_authorization_server_metadata_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .oauth()
        .get_oauth_authorization_server_metadata()
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn oauth_get_oauth_authorization_server_metadata_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/.well-known/oauth-authorization-server"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_server_metadata()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn oauth_get_oauth_authorization_server_metadata_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/.well-known/oauth-authorization-server"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_server_metadata()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn oauth_get_oauth_authorization_server_metadata_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/.well-known/oauth-authorization-server"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_server_metadata()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn oauth_get_oauth_authorization_server_metadata_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/.well-known/oauth-authorization-server"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_server_metadata()
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn oauth_get_oauth_authorization_request_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(ResponseTemplate::new(200).set_body_string("\"test_value\""))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .oauth()
        .get_oauth_authorization_request(
            sellapp::resources::oauth::GetOAuthAuthorizationRequestParams::new(
                "code".to_string(),
                "00000000-0000-0000-0000-000000000000".to_string(),
                "test_redirect_uri".to_string(),
                "test_state".to_string(),
                "test_code_challengexxxxxxxxxxxxxxxxxxxxxxxx".to_string(),
                "S256".to_string(),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn oauth_get_oauth_authorization_request_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_request(
            sellapp::resources::oauth::GetOAuthAuthorizationRequestParams::new(
                "code".to_string(),
                "00000000-0000-0000-0000-000000000000".to_string(),
                "test_redirect_uri".to_string(),
                "test_state".to_string(),
                "test_code_challengexxxxxxxxxxxxxxxxxxxxxxxx".to_string(),
                "S256".to_string(),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn oauth_get_oauth_authorization_request_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_request(
            sellapp::resources::oauth::GetOAuthAuthorizationRequestParams::new(
                "code".to_string(),
                "00000000-0000-0000-0000-000000000000".to_string(),
                "test_redirect_uri".to_string(),
                "test_state".to_string(),
                "test_code_challengexxxxxxxxxxxxxxxxxxxxxxxx".to_string(),
                "S256".to_string(),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn oauth_get_oauth_authorization_request_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_request(
            sellapp::resources::oauth::GetOAuthAuthorizationRequestParams::new(
                "code".to_string(),
                "00000000-0000-0000-0000-000000000000".to_string(),
                "test_redirect_uri".to_string(),
                "test_state".to_string(),
                "test_code_challengexxxxxxxxxxxxxxxxxxxxxxxx".to_string(),
                "S256".to_string(),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn oauth_get_oauth_authorization_request_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .get_oauth_authorization_request(
            sellapp::resources::oauth::GetOAuthAuthorizationRequestParams::new(
                "code".to_string(),
                "00000000-0000-0000-0000-000000000000".to_string(),
                "test_redirect_uri".to_string(),
                "test_state".to_string(),
                "test_code_challengexxxxxxxxxxxxxxxxxxxxxxxx".to_string(),
                "S256".to_string(),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(ResponseTemplate::new(200).set_body_string("null"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect("valid generated response");
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn oauth_approve_oauth_authorization_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().approve_oauth_authorization(sellapp::resources::oauth::ApproveOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_approve_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(ResponseTemplate::new(200).set_body_string("null"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect("valid generated response");
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn oauth_deny_oauth_authorization_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("DELETE"))
        .and(path_matcher("/oauth/authorize"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().deny_oauth_authorization(sellapp::resources::oauth::DenyOAuthAuthorizationParams::new(serde_json::from_str(include_str!("fixtures/sdk_deny_oauth_authorization_request_application_x_www_form_urlencoded.json")).expect("parse fixture for SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn oauth_exchange_oauth_token_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_exchange_oauth_token_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect("valid generated response");
}

#[tokio::test]
async fn oauth_exchange_oauth_token_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn oauth_exchange_oauth_token_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn oauth_exchange_oauth_token_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn oauth_exchange_oauth_token_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn oauth_exchange_oauth_token_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn oauth_exchange_oauth_token_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/token"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.oauth().exchange_oauth_token(sellapp::resources::oauth::ExchangeOAuthTokenParams::new(serde_json::from_str("{\"grant_type\":\"authorization_code\",\"code\":\"test_code\",\"redirect_uri\":\"test_redirect_uri\",\"code_verifier\":\"test_code_verifierxxxxxxxxxxxxxxxxxxxxxxxxx\"}").expect("parse stub"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn oauth_revoke_oauth_token_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(ResponseTemplate::new(200).set_body_string("[]"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn oauth_revoke_oauth_token_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn oauth_revoke_oauth_token_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn oauth_revoke_oauth_token_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn oauth_revoke_oauth_token_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn oauth_revoke_oauth_token_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn oauth_revoke_oauth_token_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/oauth/revoke"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .oauth()
        .revoke_oauth_token(sellapp::resources::oauth::RevokeOAuthTokenParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_revoke_oauth_token_request_application_x_www_form_urlencoded.json"
            ))
            .expect("parse fixture for SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
