// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct HighlightsMediaApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct ListParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

#[derive(Debug, Clone, Serialize)]
pub struct AddParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkAddHighlightMediaRequestMultipartFormData,
}

impl AddParams {
    /// Construct a new `AddParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkAddHighlightMediaRequestMultipartFormData) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReorderParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReorderHighlightsRequestApplicationJson,
}

impl ReorderParams {
    /// Construct a new `ReorderParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReorderHighlightsRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplaceParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplaceHighlightMediaRequestMultipartFormData,
}

impl ReplaceParams {
    /// Construct a new `ReplaceParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplaceHighlightMediaRequestMultipartFormData) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct ReplaceMetadataParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkReplaceHighlightMediaMetadataRequestApplicationJson,
}

impl ReplaceMetadataParams {
    /// Construct a new `ReplaceMetadataParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkReplaceHighlightMediaMetadataRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: SdkUpdateHighlightMediaRequestApplicationJson,
}

impl UpdateParams {
    /// Construct a new `UpdateParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: SdkUpdateHighlightMediaRequestApplicationJson) -> Self {
        Self {
            x_store: Default::default(),
            body,
        }
    }
}

#[derive(Debug, Clone, Default, Serialize)]
pub struct DeleteParams {
    /// Store slug. Required for OAuth access tokens. API keys may omit it to use their current store, or the first accessible store when no current store is selected.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(rename = "X-STORE")]
    pub x_store: Option<String>,
}

impl<'a> HighlightsMediaApi<'a> {
    /// List highlight media
    ///
    /// List media attached to a highlight in display order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn list(
        &self,
        highlight: &str,
        params: ListParams,
    ) -> Result<SdkListHighlightMediaResponseValue200ApplicationJson, Error> {
        self.list_with_options(highlight, params, None).await
    }

    /// Variant of [`Self::list`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_with_options(
        &self,
        highlight: &str,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkListHighlightMediaResponseValue200ApplicationJson, Error> {
        self.list_raw(highlight, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn list_raw(
        &self,
        highlight: &str,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkListHighlightMediaResponseValue200ApplicationJson>, Error>
    {
        let highlight = crate::client::path_segment(highlight);
        let path = format!("/v2/highlights/{highlight}/media");
        let method = http::Method::GET;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("listHighlightMedia".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "GET /v2/highlights/{highlight}/media",
            )
            .await
    }

    /// Add highlight media
    ///
    /// Upload an image or video and append it to a highlight, up to 30 media items in total. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn add(
        &self,
        highlight: &str,
        params: AddParams,
    ) -> Result<SdkAddHighlightMediaResponseValue201ApplicationJson, Error> {
        self.add_with_options(highlight, params, None).await
    }

    /// Variant of [`Self::add`] that accepts per-request [`crate::RequestOptions`].
    pub async fn add_with_options(
        &self,
        highlight: &str,
        params: AddParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkAddHighlightMediaResponseValue201ApplicationJson, Error> {
        self.add_raw(highlight, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn add_raw(
        &self,
        highlight: &str,
        params: AddParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkAddHighlightMediaResponseValue201ApplicationJson>, Error>
    {
        let highlight = crate::client::path_segment(highlight);
        let path = format!("/v2/highlights/{highlight}/media");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("addHighlightMedia".to_string());
        merged.body_encoding = crate::BodyEncoding::Multipart;
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/highlights/{highlight}/media",
            )
            .await
    }

    /// Reorder highlight media
    ///
    /// Replace the complete media order for a highlight with the supplied media IDs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn reorder(
        &self,
        highlight: &str,
        params: ReorderParams,
    ) -> Result<SdkReorderHighlightMediaResponseValue200ApplicationJson, Error> {
        self.reorder_with_options(highlight, params, None).await
    }

    /// Variant of [`Self::reorder`] that accepts per-request [`crate::RequestOptions`].
    pub async fn reorder_with_options(
        &self,
        highlight: &str,
        params: ReorderParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReorderHighlightMediaResponseValue200ApplicationJson, Error> {
        self.reorder_raw(highlight, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn reorder_raw(
        &self,
        highlight: &str,
        params: ReorderParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReorderHighlightMediaResponseValue200ApplicationJson>, Error>
    {
        let highlight = crate::client::path_segment(highlight);
        let path = format!("/v2/highlights/{highlight}/media/reorder");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("reorderHighlightMedia".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/highlights/{highlight}/media/reorder",
            )
            .await
    }

    /// Replace highlight media
    ///
    /// Replace highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn replace(
        &self,
        highlight: &str,
        media: &str,
        params: ReplaceParams,
    ) -> Result<SdkReplaceHighlightMediaResponseValue200ApplicationJson, Error> {
        self.replace_with_options(highlight, media, params, None)
            .await
    }

    /// Variant of [`Self::replace`] that accepts per-request [`crate::RequestOptions`].
    pub async fn replace_with_options(
        &self,
        highlight: &str,
        media: &str,
        params: ReplaceParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplaceHighlightMediaResponseValue200ApplicationJson, Error> {
        self.replace_raw(highlight, media, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn replace_raw(
        &self,
        highlight: &str,
        media: &str,
        params: ReplaceParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkReplaceHighlightMediaResponseValue200ApplicationJson>, Error>
    {
        let highlight = crate::client::path_segment(highlight);
        let media = crate::client::path_segment(media);
        let path = format!("/v2/highlights/{highlight}/media/{media}");
        let method = http::Method::POST;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replaceHighlightMedia".to_string());
        merged.body_encoding = crate::BodyEncoding::Multipart;
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "POST /v2/highlights/{highlight}/media/{media}",
            )
            .await
    }

    /// Update highlight media
    ///
    /// Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn replace_metadata(
        &self,
        highlight: &str,
        media: &str,
        params: ReplaceMetadataParams,
    ) -> Result<SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson, Error> {
        self.replace_metadata_with_options(highlight, media, params, None)
            .await
    }

    /// Variant of [`Self::replace_metadata`] that accepts per-request [`crate::RequestOptions`].
    pub async fn replace_metadata_with_options(
        &self,
        highlight: &str,
        media: &str,
        params: ReplaceMetadataParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson, Error> {
        self.replace_metadata_raw(highlight, media, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn replace_metadata_raw(
        &self,
        highlight: &str,
        media: &str,
        params: ReplaceMetadataParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<
        crate::RawResponse<SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson>,
        Error,
    > {
        let highlight = crate::client::path_segment(highlight);
        let media = crate::client::path_segment(media);
        let path = format!("/v2/highlights/{highlight}/media/{media}");
        let method = http::Method::PUT;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("replaceHighlightMediaMetadata".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PUT /v2/highlights/{highlight}/media/{media}",
            )
            .await
    }

    /// Update highlight media
    ///
    /// Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn update(
        &self,
        highlight: &str,
        media: &str,
        params: UpdateParams,
    ) -> Result<SdkUpdateHighlightMediaResponseValue200ApplicationJson, Error> {
        self.update_with_options(highlight, media, params, None)
            .await
    }

    /// Variant of [`Self::update`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_with_options(
        &self,
        highlight: &str,
        media: &str,
        params: UpdateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkUpdateHighlightMediaResponseValue200ApplicationJson, Error> {
        self.update_raw(highlight, media, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn update_raw(
        &self,
        highlight: &str,
        media: &str,
        params: UpdateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkUpdateHighlightMediaResponseValue200ApplicationJson>, Error>
    {
        let highlight = crate::client::path_segment(highlight);
        let media = crate::client::path_segment(media);
        let path = format!("/v2/highlights/{highlight}/media/{media}");
        let method = http::Method::PATCH;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("updateHighlightMedia".to_string());
        let options = Some(&merged);
        self.client
            .request_with_body_schema_opts_raw(
                method,
                &path,
                &params,
                Some(&params.body),
                options,
                "PATCH /v2/highlights/{highlight}/media/{media}",
            )
            .await
    }

    /// Delete highlight media
    ///
    /// Delete highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
    pub async fn delete(
        &self,
        highlight: &str,
        media: &str,
        params: DeleteParams,
    ) -> Result<SdkDeleteHighlightMediaResponseValue200ApplicationJson, Error> {
        self.delete_with_options(highlight, media, params, None)
            .await
    }

    /// Variant of [`Self::delete`] that accepts per-request [`crate::RequestOptions`].
    pub async fn delete_with_options(
        &self,
        highlight: &str,
        media: &str,
        params: DeleteParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<SdkDeleteHighlightMediaResponseValue200ApplicationJson, Error> {
        self.delete_raw(highlight, media, params, options)
            .await
            .map(|response| response.data)
    }

    /// Returns the typed result together with status, headers, and request ID.
    pub async fn delete_raw(
        &self,
        highlight: &str,
        media: &str,
        params: DeleteParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<crate::RawResponse<SdkDeleteHighlightMediaResponseValue200ApplicationJson>, Error>
    {
        let highlight = crate::client::path_segment(highlight);
        let media = crate::client::path_segment(media);
        let path = format!("/v2/highlights/{highlight}/media/{media}");
        let method = http::Method::DELETE;
        let mut merged = options.cloned().unwrap_or_default();
        merged.idempotency_supported = false;
        merged.operation_id = Some("deleteHighlightMedia".to_string());
        let options = Some(&merged);
        self.client
            .request_with_query_schema_opts_raw(
                method,
                &path,
                &params,
                options,
                "DELETE /v2/highlights/{highlight}/media/{media}",
            )
            .await
    }
}
