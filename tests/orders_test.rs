// This file is auto-generated by oagen. Do not edit.

mod common;

use sellapp::Error;
use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn orders_list_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_orders_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .list(sellapp::resources::orders::ListParams::default())
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_list_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list(sellapp::resources::orders::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_list_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list(sellapp::resources::orders::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_list_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list(sellapp::resources::orders::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_list_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list(sellapp::resources::orders::ListParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_list_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .orders()
        .list(sellapp::resources::orders::ListParams::default())
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn orders_create_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_order_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_create_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_create_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_create_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_create_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_create_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_create_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create(sellapp::resources::orders::CreateParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_search_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_search_orders_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_search_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_search_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_search_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_search_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_search_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_search_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_search_empty_page() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/search"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{\"data\":[],\"links\":{\"first\":\"https://sell.app/api/v1/blacklists?page=1\",\"last\":\"https://sell.app/api/v1/blacklists?page=4\",\"prev\":\"test_prev\",\"next\":\"https://sell.app/api/v1/blacklists?page=2\"},\"meta\":{\"current_page\":1,\"from\":1,\"last_page\":4,\"links\":[{\"url\":\"test_url\",\"label\":\"test_label\",\"active\":false}],\"path\":\"https://sell.app/api/v1/blacklists\",\"per_page\":15,\"to\":15,\"total\":57}}"))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let resp = client
        .orders()
        .search(sellapp::resources::orders::SearchParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_search_orders_request_application_json.json"
            ))
            .expect("parse fixture for SdkSearchOrdersRequestApplicationJson"),
        ))
        .await
        .expect("expected success");
    assert!(resp.data.is_empty(), "expected empty data array");
}

#[tokio::test]
async fn orders_get_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_get_order_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .get("test_id", sellapp::resources::orders::GetParams::default())
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_get_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .get("test_id", sellapp::resources::orders::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_get_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .get("test_id", sellapp::resources::orders::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_get_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .get("test_id", sellapp::resources::orders::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_get_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .get("test_id", sellapp::resources::orders::GetParams::default())
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_update_status_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_update_order_status_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_update_status_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_update_status_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_update_status_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_update_status_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_update_status_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_update_status_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PATCH"))
        .and(path_matcher("/v2/orders/test_id/status"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .update_status(
            "test_id",
            sellapp::resources::orders::UpdateStatusParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_update_order_status_request_application_json.json"
                ))
                .expect("parse fixture for SdkUpdateOrderStatusRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_create_checkout_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_order_checkout_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_create_checkout_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_create_checkout_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_create_checkout_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_create_checkout_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_create_checkout_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_create_checkout_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/checkout"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_checkout(
            "test_id",
            sellapp::resources::orders::CreateCheckoutParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_checkout_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderCheckoutRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_create_replacement_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_order_replacement_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_create_replacement_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_create_replacement_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_create_replacement_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_create_replacement_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_create_replacement_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_create_replacement_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/replacements"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_replacement(
            "test_id",
            sellapp::resources::orders::CreateReplacementParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_replacement_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderReplacementRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_create_refund_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_order_refund_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_create_refund_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_create_refund_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_create_refund_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_create_refund_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_create_refund_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_create_refund_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/refunds"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_refund(
            "test_id",
            sellapp::resources::orders::CreateRefundParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_create_order_refund_request_application_json.json"
                ))
                .expect("parse fixture for SdkCreateOrderRefundRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_retry_fulfillment_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_retry_order_fulfillment_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_retry_fulfillment_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_retry_fulfillment_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_retry_fulfillment_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_retry_fulfillment_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_retry_fulfillment_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_retry_fulfillment_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_fulfillment(
            "test_id",
            sellapp::resources::orders::RetryFulfillmentParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_fulfillment_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderFulfillmentRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_retry_order_dynamic_delivery_response_value_202_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_retry_dynamic_delivery_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/dynamic-delivery-retries"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .retry_dynamic_delivery(
            "test_id",
            sellapp::resources::orders::RetryDynamicDeliveryParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_retry_order_dynamic_delivery_request_application_json.json"
                ))
                .expect("parse fixture for SdkRetryOrderDynamicDeliveryRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!("fixtures/sdk_send_order_fulfillment_notifications_response_value_200_application_json.json")))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect("valid generated response");
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_send_fulfillment_notifications_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/fulfillment-notifications"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client.orders().send_fulfillment_notifications("test_id", sellapp::resources::orders::SendFulfillmentNotificationsParams::new(serde_json::from_str(include_str!("fixtures/sdk_send_order_fulfillment_notifications_request_application_json.json")).expect("parse fixture for SdkSendOrderFulfillmentNotificationsRequestApplicationJson"))).await.expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_list_deliverables_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id/deliverables"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_list_order_deliverables_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .list_deliverables(
            "test_id",
            sellapp::resources::orders::ListDeliverablesParams::default(),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_list_deliverables_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list_deliverables(
            "test_id",
            sellapp::resources::orders::ListDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_list_deliverables_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list_deliverables(
            "test_id",
            sellapp::resources::orders::ListDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_list_deliverables_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list_deliverables(
            "test_id",
            sellapp::resources::orders::ListDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_list_deliverables_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher("/v2/orders/test_id/deliverables"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .list_deliverables(
            "test_id",
            sellapp::resources::orders::ListDeliverablesParams::default(),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_create_from_wallet_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_create_wallet_order_response_value_201_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_create_from_wallet_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_create_from_wallet_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_create_from_wallet_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_create_from_wallet_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_create_from_wallet_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_create_from_wallet_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/wallet"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .create_from_wallet(sellapp::resources::orders::CreateFromWalletParams::new(
            serde_json::from_str(include_str!(
                "fixtures/sdk_create_wallet_order_request_application_json.json"
            ))
            .expect("parse fixture for SdkCreateWalletOrderRequestApplicationJson"),
        ))
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}

#[tokio::test]
async fn orders_pay_from_wallet_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/sdk_pay_order_from_wallet_response_value_200_application_json.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect("valid generated response");
}

#[tokio::test]
async fn orders_pay_from_wallet_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Authentication(api) => api.as_ref(),
        other => panic!("expected Error::Authentication, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn orders_pay_from_wallet_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn orders_pay_from_wallet_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn orders_pay_from_wallet_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn orders_pay_from_wallet_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn orders_pay_from_wallet_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("POST"))
        .and(path_matcher("/v2/orders/test_id/wallet-payments"))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .orders()
        .pay_from_wallet(
            "test_id",
            sellapp::resources::orders::PayFromWalletParams::new(
                serde_json::from_str(include_str!(
                    "fixtures/sdk_pay_order_from_wallet_request_application_json.json"
                ))
                .expect("parse fixture for SdkPayOrderFromWalletRequestApplicationJson"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
